# frozen_string_literal: true

# Demonstrates a "custom versions association name". Instead of the assocation
# being named `versions`, it will be named `brick_versions`.
class Document < ActiveRecord::Base
end
