# frozen_string_literal: true

class Truck < Vehicle
  # This STI child class is from the virtual brick-created model Vehicle.
  # Of the sub-classes of `Vehicle`, only `Car` and `Bicycle` are versioned.
end
